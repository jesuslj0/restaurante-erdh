---

import "../styles/global.css";

interface Props {
  restaurantName: string;
  pageTitle?: string;
}

const { restaurantName, pageTitle } = Astro.props;

---


<!doctype html>
<html lang="es">
	<head>
		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover"/>
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<link rel="icon" href="/favicon.ico" />
		<meta name="generator" content={Astro.generator} />
		<title>{pageTitle ?? restaurantName}</title>
		<link rel="preconnect" href="https://fonts.googleapis.com" />
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
		<link rel="stylesheet" href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:ital,wght@0,400;0,500;0,600;0,700;1,400;1,600&family=Jost:wght@400;500;600&display=swap" />
	</head>
	<body>
		<slot name="header"/>
		<main>
			<slot name="hero" />
			<slot name="showcase" />
			<slot name="about" />
			<slot name="reservations" />
			<slot />
		</main>
		<slot name="footer" />
	</body>
</html>

<script>
	import '../scripts/lenis.js';
</script>

<style>
	html,
	body {
		margin: 0;
		width: 100%;
		height: 100%;
		display: flex;
		flex-direction: column;
	}

	main {
		flex-grow: 1;
	}
</style>
